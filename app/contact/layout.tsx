import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Quantum Limited | Initialize Connection Protocol",
  description:
    "Bridge the gap between vision and execution. Contact our engineering team for technical requirements and global node access.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
